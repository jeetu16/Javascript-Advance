// strict mode = with the strict mode you can not use the variable without defining it. Always use the strict mode at the top of the file 'use strict'

// without strict mode

// y = 5;   // no error
// console.log(y); //

'use strict'

x = 4   //
console.log(x)
